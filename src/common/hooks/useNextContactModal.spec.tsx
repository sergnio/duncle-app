import { render } from "@testing-library/react";
import useNextContactModal from "./useNextContactModal";

describe("useNextContactModal tests", () => {
  it("should open a modal when calling the handleScheduleNextContact fn", () => {});

  it("should call the save function when hitting the save button", () => {});

  it("should call the optional bacllback if there is one", () => {});

  it("should close the modal if we hit the close button", () => {});

  it("should NOT call any function if we close out", () => {});
});
