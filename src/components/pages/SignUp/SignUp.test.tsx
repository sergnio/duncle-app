import { render } from "@testing-library/react";
import React, { Fragment } from "react";

// only exists to fix github actions pipeline
describe("SignUp.test.tsx tests", () => {
  it("should render", () => {
    render(<Fragment />);
  });
});
